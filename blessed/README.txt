aio.sh the current big script compines nearly all the separate small file, which constitute it.

All the small files now moved into working/von-blesses/

when i clone the repo from Github It will contain just what is not in .gitignore hence just aio.sh 
whereas nearly 2 files  in local original Directory. it make difficult to know what else included 